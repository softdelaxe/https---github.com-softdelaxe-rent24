import '../../../core/app_export.dart';/// This class is used in the [selectstatecomponent_item_widget] screen.
class SelectstatecomponentItemModel {SelectstatecomponentItemModel({this.selectedState, this.stateName, this.id, }) { selectedState = selectedState  ?? "Select State";stateName = stateName  ?? "Anambra";id = id  ?? ""; }

String? selectedState;

String? stateName;

String? id;

 }
